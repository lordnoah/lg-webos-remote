version: '3.3'

services:
  lg-remote:
    build: .
    container_name: lg-remote
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./public:/app/public
    environment:
      - TV_IP=192.168.1.29
